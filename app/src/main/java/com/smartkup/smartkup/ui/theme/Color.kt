package com.smartkup.smartkup.ui.theme

import androidx.compose.ui.graphics.Color

// === CATPPUCCIN MOCHA (DARK MODE) ===
val MochaBase = Color(0xFF1e1e2e)        // Main background
val MochaSurface = Color(0xFF313244)     // Cards, Dialogs, Menus
val MochaPrimary = Color(0xFF89b4fa)     // Buttons, Checkboxes (Blue)
val MochaSecondary = Color(0xFFf5c2e7)   // Accents (Pink)
val MochaText = Color(0xFFcdd6f4)        // Standard text
val MochaSubtext = Color(0xFFa6adc8)     // Secondary text (units, unselected)
val MochaError = Color(0xFFf38ba8)       // Red for errors/deletes

// === CATPPUCCIN LATTE (LIGHT MODE) ===
val LatteBase = Color(0xFFeff1f5)        // Main background
val LatteSurface = Color(0xFFe6e9ef)     // Cards, Dialogs, Menus
val LattePrimary = Color(0xFF1e66f5)     // Buttons, Checkboxes (Blue)
val LatteSecondary = Color(0xFFea76cb)   // Accents (Pink)
val LatteText = Color(0xFF4c4f69)        // Standard text
val LatteSubtext = Color(0xFF6c6f85)     // Secondary text
val LatteError = Color(0xFFd20f39)       // Red for errors/deletes